# ── Secrets, keys, and local config — never commit ──────────────
.env
.env.*
!.env.example
keys/
runtime/
*.pem
*.fp
.claude/settings.local.json

# ── Encrypted output and signature files ────────────────────────
*.enc
*.sig

# ── Audit log (runtime artifact) ────────────────────────────────
audit_log.json*

# ── User store (runtime artifact — contains hashed credentials) ─
users.json
runtime/users.json

# ── Runtime workspace (generated encrypt/decrypt scaffolding) ───
workspace/

# ── Python build / cache artifacts ──────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
build/
dist/

# ── Virtual environments ────────────────────────────────────────
.venv/
venv/
env/

# ── Test artifacts ──────────────────────────────────────────────
*.dec
*.decrypted
.pytest_cache/

# ── Editor / OS artifacts ───────────────────────────────────────
*.swp
*.swo
*~
.DS_Store
__MACOSX/

# ── AI-session task briefs (not part of the public artifact) ────
instruction*.md
instuction*.md
